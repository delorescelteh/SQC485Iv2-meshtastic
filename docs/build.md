# 編譯 Meshtastic-device

在 repo 根目錄：

```bash
pio run -e heltec-ht62-esp32c3-sx1262
```

成功後產物通常在：

```text
.pio/build/heltec-ht62-esp32c3-sx1262/
```

常見會生成：

- `firmware-... .bin`
- `... .factory.bin`
